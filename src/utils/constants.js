export const API_URL = "http://127.0.0.1:8090";

export const NO_IMAGE = "img/no-img.png";

export const NO_AVATAR = "img/no-avatar.png";

export const NUMBER_OF_IMAGES = 5;

export const validEmail = new RegExp(/^[\w]{1}[\w-.]*@[\w-]+\.[a-z]{2,3}$/i);

export const validPasswordLength = 8;

export const valid = new RegExp(/[^0-9+]/i);

export const validPrice = new RegExp(/^([0-9]*[.]?)?(\d{1,2})?$/i);
